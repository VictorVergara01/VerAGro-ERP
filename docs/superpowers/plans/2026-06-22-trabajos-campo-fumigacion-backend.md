# Trabajos de Campo (Fumigación) — Backend Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reconstruir el backend del módulo de Trabajos de Campo limpio y enfocado en fumigación, eliminando la carga heredada (esparcido/quintales, producto único, clima, GPS) y dejando reservado el enum `spreading` y las unidades de sólidos.

**Architecture:** Se modifica el modelo `FieldJob` existente (drop de campos heredados, add de `crop`/`crop_other`, default de hectáreas a 1) vía una migración aditiva `0003` (no se reescribe el historial de migraciones ya pusheado). Serializer y viewset se limpian; `create_invoice_from_field_job` deja de referenciar `quintals`. La calculadora (`calculate_mix`) y `FieldJobProduct` (con sus 4 unidades) NO cambian.

**Tech Stack:** Django 5 + DRF, pytest, Docker (`docker compose exec -T backend ...`).

## Global Constraints

- Rama `V2.0`; **no** mergear a master sin pedido explícito.
- Unidades de químico en la UI de fumigación: **L/ha y cc/ha**. El modelo `FieldJobProduct.Unit` conserva las 4 (`L/ha, cc/ha, kg/ha, g/ha`) reservando kg/ha y g/ha para sólidos.
- Tanque default **200 L** (modificable); hectáreas default **1** (modificable).
- Hasta **10 químicos** por trabajo (error 400 si se envían más).
- Cultivo: choices `rice`/`corn`/`pasture`/`other` (labels Arroz/Maíz/Pasto/Otros), default `rice`; `crop_other` texto libre para "otros".
- `total = hectares × unit_price` (solo fumigación por ahora).
- Permisos: escritura `admin`/`technician`/`sales`, lectura para autenticados.
- Tras agregar migraciones: `docker compose exec -T backend python manage.py migrate` antes de probar en vivo.
- Tests se ejecutan con `docker compose exec -T backend pytest`.
- Commits en español, trailer `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`.

## File Structure

- `backend/apps/field_jobs/models.py` — `FieldJob` limpio (Crop choices, crop_other, sin campos heredados) + `FieldJobProduct` (sin cambios). Responsable del esquema y `recalculate_total`.
- `backend/apps/field_jobs/migrations/0003_*.py` — migración de transición (generada por makemigrations).
- `backend/apps/field_jobs/serializers.py` — `FieldJobSerializer` limpio + tope de 10 químicos + `crop_display`/`crop_other`.
- `backend/apps/field_jobs/views.py` — `search_fields` y permisos.
- `backend/apps/billing/services.py` — `create_invoice_from_field_job` sin `quintals`.
- `backend/apps/field_jobs/tests/` — `test_models.py`, `test_api.py` reescritos al esquema nuevo; `test_mix.py`/`test_services.py`/`test_products_*` se mantienen (calculate_mix y FieldJobProduct no cambian).

---

### Task 1: Modelo limpio + migración

**Files:**
- Modify: `backend/apps/field_jobs/models.py`
- Create: `backend/apps/field_jobs/migrations/0003_*.py` (vía makemigrations)
- Test: `backend/apps/field_jobs/tests/test_models.py` (reescribir)

**Interfaces:**
- Produces:
  - `FieldJob` con: `number, job_type, status, customer, equipment, technician, scheduled_date, done_date, location, crop, crop_other, hectares, unit_price, total, tank_volume_liters, water_per_hectare, notes, created_by` + timestamps.
  - `FieldJob.Crop` TextChoices: `RICE="rice"`, `CORN="corn"`, `PASTURE="pasture"`, `OTHER="other"`.
  - `FieldJob.JobType` (`fumigation`/`spreading`), `FieldJob.Status` (`scheduled`/`done`/`invoiced`/`cancelled`) — sin cambios.
  - `recalculate_total()` → `total = hectares × unit_price`.
  - `FieldJobProduct` sin cambios (4 unidades).

- [ ] **Step 1: Reescribir los tests de modelo**

Reemplazar todo el contenido de `backend/apps/field_jobs/tests/test_models.py` por:

```python
import pytest
from decimal import Decimal

from apps.customers.models import Customer
from apps.field_jobs.models import FieldJob

pytestmark = pytest.mark.django_db


def _customer():
    return Customer.objects.create(name="Finca La Esperanza")


def test_number_autogenerated_on_save():
    job = FieldJob.objects.create(customer=_customer())
    assert job.number == f"TC-{job.pk:06d}"


def test_defaults_fumigation_rice_one_hectare():
    job = FieldJob.objects.create(customer=_customer())
    assert job.job_type == FieldJob.JobType.FUMIGATION
    assert job.status == FieldJob.Status.SCHEDULED
    assert job.crop == FieldJob.Crop.RICE
    assert job.hectares == Decimal("1")


def test_recalculate_total_is_hectares_times_price():
    job = FieldJob.objects.create(
        customer=_customer(), hectares=Decimal("15"), unit_price=Decimal("20")
    )
    job.recalculate_total()
    assert job.total == Decimal("300.00")


def test_crop_other_stores_free_text():
    job = FieldJob.objects.create(
        customer=_customer(), crop=FieldJob.Crop.OTHER, crop_other="Sandía"
    )
    assert job.get_crop_display() == "Otros"
    assert job.crop_other == "Sandía"
```

- [ ] **Step 2: Correr los tests para verlos fallar**

Run: `docker compose exec -T backend pytest apps/field_jobs/tests/test_models.py -q`
Expected: FAIL (el modelo todavía tiene `crop` como texto sin choices, hectares default 0, y campos heredados; algún test de crop/hectares falla o el import de `Crop` no existe).

- [ ] **Step 3: Reescribir el modelo limpio**

Reemplazar todo el contenido de `backend/apps/field_jobs/models.py` por:

```python
from decimal import Decimal

from django.db import models
from django.utils import timezone

from apps.core.models import TimeStampedModel


class FieldJob(TimeStampedModel):
    class JobType(models.TextChoices):
        FUMIGATION = "fumigation", "Fumigación"
        SPREADING = "spreading", "Esparcido / abono"  # reservado para sólidos

    class Status(models.TextChoices):
        SCHEDULED = "scheduled", "Programado"
        DONE = "done", "Hecho"
        INVOICED = "invoiced", "Facturado"
        CANCELLED = "cancelled", "Cancelado"

    class Crop(models.TextChoices):
        RICE = "rice", "Arroz"
        CORN = "corn", "Maíz"
        PASTURE = "pasture", "Pasto"
        OTHER = "other", "Otros"

    number = models.CharField(max_length=30, unique=True, blank=True)
    job_type = models.CharField(
        max_length=20, choices=JobType.choices, default=JobType.FUMIGATION
    )
    status = models.CharField(
        max_length=20, choices=Status.choices, default=Status.SCHEDULED
    )
    customer = models.ForeignKey(
        "customers.Customer", on_delete=models.PROTECT, related_name="field_jobs"
    )
    equipment = models.ForeignKey(
        "equipment.Equipment",
        on_delete=models.PROTECT,
        null=True,
        blank=True,
        related_name="field_jobs",
    )
    technician = models.ForeignKey(
        "users.User",
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        related_name="field_jobs",
    )
    scheduled_date = models.DateField(default=timezone.localdate)
    done_date = models.DateField(null=True, blank=True)
    location = models.CharField(max_length=255, blank=True)
    crop = models.CharField(max_length=20, choices=Crop.choices, default=Crop.RICE)
    crop_other = models.CharField(max_length=100, blank=True)
    hectares = models.DecimalField(max_digits=10, decimal_places=4, default=1)
    unit_price = models.DecimalField(max_digits=12, decimal_places=2, default=0)
    total = models.DecimalField(max_digits=14, decimal_places=2, default=0)
    tank_volume_liters = models.DecimalField(
        max_digits=10, decimal_places=2, null=True, blank=True
    )
    water_per_hectare = models.DecimalField(
        max_digits=10, decimal_places=2, null=True, blank=True
    )
    notes = models.TextField(blank=True)
    created_by = models.ForeignKey(
        "users.User", on_delete=models.SET_NULL, null=True, blank=True, related_name="+"
    )

    class Meta:
        ordering = ("-scheduled_date", "-created_at")

    def __str__(self):
        return self.number or f"TC sin número (#{self.pk})"

    def recalculate_total(self):
        # Solo fumigación por ahora; el módulo de sólidos ajustará esto.
        self.total = (self.hectares or Decimal("0")) * (self.unit_price or Decimal("0"))

    def save(self, *args, **kwargs):
        super().save(*args, **kwargs)
        if not self.number:
            self.number = f"TC-{self.pk:06d}"
            super().save(update_fields=["number"])


class FieldJobProduct(TimeStampedModel):
    class Unit(models.TextChoices):
        L_HA = "L/ha", "L/ha"
        CC_HA = "cc/ha", "cc/ha"
        KG_HA = "kg/ha", "kg/ha"  # reservado para sólidos
        G_HA = "g/ha", "g/ha"  # reservado para sólidos

    field_job = models.ForeignKey(
        FieldJob, on_delete=models.CASCADE, related_name="products"
    )
    name = models.CharField(max_length=150)
    dose_per_hectare = models.DecimalField(max_digits=10, decimal_places=4, default=0)
    unit = models.CharField(max_length=10, choices=Unit.choices, default=Unit.L_HA)

    class Meta:
        ordering = ("id",)

    def __str__(self):
        return f"{self.name} ({self.dose_per_hectare} {self.unit})"
```

- [ ] **Step 4: Generar la migración**

Run: `docker compose exec -T backend python manage.py makemigrations field_jobs`
Expected: crea `0003_*.py` con operaciones que **remueven** `quintals, applied_product, application_rate, application_rate_unit, latitude, longitude, wind_speed_kmh, temperature_celsius, humidity_percentage, weather_notes`, **agregan** `crop_other`, y **alteran** `crop` (a choices, max_length 20) y `hectares` (default 1). Si pide un valor por defecto para alguna columna, no debería (todas las removidas o con default). Revisar el archivo generado.

- [ ] **Step 5: Aplicar la migración y correr los tests**

Run:
```
docker compose exec -T backend python manage.py migrate field_jobs
docker compose exec -T backend pytest apps/field_jobs/tests/test_models.py -q
```
Expected: migración OK; 4 tests PASS.

- [ ] **Step 6: Commit**

```bash
git add backend/apps/field_jobs/models.py backend/apps/field_jobs/migrations/0003_*.py backend/apps/field_jobs/tests/test_models.py
git commit -m "feat(field-jobs): modelo de fumigacion limpio (cultivo, sin campos heredados)

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 2: Serializer limpio + tope de 10 químicos

**Files:**
- Modify: `backend/apps/field_jobs/serializers.py`
- Test: `backend/apps/field_jobs/tests/test_api.py` (reescribir)

**Interfaces:**
- Consumes: `FieldJob`, `FieldJob.Crop`, `FieldJobProduct` (Task 1).
- Produces: `FieldJobSerializer` con campos limpios + `crop_display` (read-only label) + `crop_other`; validación `products` ≤ 10.

- [ ] **Step 1: Reescribir los tests de API del serializer/CRUD**

Reemplazar todo el contenido de `backend/apps/field_jobs/tests/test_api.py` por:

```python
import pytest
from decimal import Decimal

from rest_framework.test import APIClient

from apps.customers.models import Customer
from apps.field_jobs.models import FieldJob
from apps.users.models import User

pytestmark = pytest.mark.django_db


@pytest.fixture
def admin_client():
    user = User.objects.create_user(
        email="admin@test.com", password="x", role="admin", first_name="Ad", last_name="Min"
    )
    client = APIClient()
    client.force_authenticate(user=user)
    return client


@pytest.fixture
def customer():
    return Customer.objects.create(name="Finca La Esperanza")


def test_create_with_products_and_crop_other(admin_client, customer):
    payload = {
        "customer": customer.id,
        "crop": "other",
        "crop_other": "Sandía",
        "hectares": "15",
        "unit_price": "20",
        "water_per_hectare": "20",
        "tank_volume_liters": "200",
        "products": [
            {"name": "Glifosato", "dose_per_hectare": "10", "unit": "L/ha"},
            {"name": "Adherente", "dose_per_hectare": "50", "unit": "cc/ha"},
        ],
    }
    res = admin_client.post("/api/field-jobs/", payload, format="json")
    assert res.status_code == 201, res.content
    body = res.json()
    assert body["number"].startswith("TC-")
    assert body["crop_display"] == "Otros"
    assert body["crop_other"] == "Sandía"
    assert body["total"] == "300.00"
    assert len(body["products"]) == 2


def test_rejects_more_than_ten_products(admin_client, customer):
    products = [
        {"name": f"Q{i}", "dose_per_hectare": "1", "unit": "L/ha"} for i in range(11)
    ]
    res = admin_client.post(
        "/api/field-jobs/",
        {"customer": customer.id, "products": products},
        format="json",
    )
    assert res.status_code == 400
    assert "products" in res.json()


def test_update_replaces_products(admin_client, customer):
    job = FieldJob.objects.create(customer=customer, hectares=Decimal("1"))
    res = admin_client.patch(
        f"/api/field-jobs/{job.id}/",
        {"products": [{"name": "Nuevo", "dose_per_hectare": "2", "unit": "L/ha"}]},
        format="json",
    )
    assert res.status_code == 200
    assert [p["name"] for p in res.json()["products"]] == ["Nuevo"]
```

- [ ] **Step 2: Correr los tests para verlos fallar**

Run: `docker compose exec -T backend pytest apps/field_jobs/tests/test_api.py -q`
Expected: FAIL (`crop_display` no existe aún en el serializer; el tope de 10 no está; el serializer todavía declara campos heredados ya removidos del modelo → error al cargar/serializar).

- [ ] **Step 3: Reescribir el serializer limpio**

Reemplazar todo el contenido de `backend/apps/field_jobs/serializers.py` por:

```python
from rest_framework import serializers

from .models import FieldJob, FieldJobProduct


class FieldJobProductSerializer(serializers.ModelSerializer):
    class Meta:
        model = FieldJobProduct
        fields = ("id", "name", "dose_per_hectare", "unit")


class FieldJobSerializer(serializers.ModelSerializer):
    products = FieldJobProductSerializer(many=True, required=False)
    customer_name = serializers.CharField(source="customer.name", read_only=True)
    equipment_name = serializers.CharField(
        source="equipment.name", read_only=True, default=""
    )
    technician_name = serializers.CharField(
        source="technician.full_name", read_only=True, default=""
    )
    job_type_display = serializers.CharField(
        source="get_job_type_display", read_only=True
    )
    status_display = serializers.CharField(source="get_status_display", read_only=True)
    crop_display = serializers.CharField(source="get_crop_display", read_only=True)
    invoice_number = serializers.SerializerMethodField()

    def get_invoice_number(self, obj):
        invoice = next(
            (inv for inv in obj.invoices.all() if inv.status != "cancelled"),
            None,
        )
        return invoice.invoice_number if invoice else None

    def validate_products(self, value):
        if len(value) > 10:
            raise serializers.ValidationError("Máximo 10 químicos por trabajo.")
        return value

    class Meta:
        model = FieldJob
        fields = (
            "id",
            "number",
            "job_type",
            "job_type_display",
            "status",
            "status_display",
            "customer",
            "customer_name",
            "equipment",
            "equipment_name",
            "technician",
            "technician_name",
            "scheduled_date",
            "done_date",
            "location",
            "crop",
            "crop_display",
            "crop_other",
            "products",
            "hectares",
            "unit_price",
            "total",
            "tank_volume_liters",
            "water_per_hectare",
            "notes",
            "invoice_number",
            "created_by",
            "created_at",
            "updated_at",
        )
        read_only_fields = (
            "id",
            "number",
            "status",
            "done_date",
            "total",
            "created_by",
            "created_at",
            "updated_at",
        )

    def create(self, validated_data):
        products = validated_data.pop("products", [])
        job = super().create(validated_data)
        for product in products:
            FieldJobProduct.objects.create(field_job=job, **product)
        return job

    def update(self, instance, validated_data):
        products = validated_data.pop("products", None)
        job = super().update(instance, validated_data)
        if products is not None:
            job.products.all().delete()
            for product in products:
                FieldJobProduct.objects.create(field_job=job, **product)
        return job
```

- [ ] **Step 4: Correr los tests**

Run: `docker compose exec -T backend pytest apps/field_jobs/tests/test_api.py -q`
Expected: 3 tests PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/apps/field_jobs/serializers.py backend/apps/field_jobs/tests/test_api.py
git commit -m "feat(field-jobs): serializer limpio con cultivo y tope de 10 quimicos

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 3: Viewset (búsqueda/permisos) + fix de facturación

**Files:**
- Modify: `backend/apps/field_jobs/views.py`
- Modify: `backend/apps/billing/services.py:235-275` (`create_invoice_from_field_job`)
- Test: `backend/apps/field_jobs/tests/test_actions.py` (crear)

**Interfaces:**
- Consumes: `FieldJobSerializer` (Task 2), `calculate_mix`/`mark_done`/`cancel_job` (sin cambios), `create_invoice_from_field_job`.
- Produces: viewset con `search_fields` sin `applied_product`, permiso de escritura admin/technician/sales; `create_invoice_from_field_job` solo fumigación (sin `quintals`).

- [ ] **Step 1: Escribir los tests de acciones/búsqueda**

Crear `backend/apps/field_jobs/tests/test_actions.py`:

```python
import pytest
from decimal import Decimal

from rest_framework.test import APIClient

from apps.customers.models import Customer
from apps.field_jobs.models import FieldJob
from apps.users.models import User

pytestmark = pytest.mark.django_db


@pytest.fixture
def admin_client():
    user = User.objects.create_user(
        email="a@test.com", password="x", role="general_admin", full_name="A B"
    )
    client = APIClient()
    client.force_authenticate(user=user)
    return client


@pytest.fixture
def customer():
    return Customer.objects.create(name="Finca")


def test_search_by_crop(admin_client, customer):
    FieldJob.objects.create(customer=customer, crop=FieldJob.Crop.CORN)
    res = admin_client.get("/api/field-jobs/?search=corn")
    assert res.status_code == 200
    assert res.json()["count"] >= 1


def test_mark_done_then_generate_invoice(admin_client, customer):
    job = FieldJob.objects.create(
        customer=customer, hectares=Decimal("10"), unit_price=Decimal("20")
    )
    job.recalculate_total()
    job.save()
    assert admin_client.post(f"/api/field-jobs/{job.id}/mark-done/").status_code == 200
    res = admin_client.post(f"/api/field-jobs/{job.id}/generate-invoice/")
    assert res.status_code == 201, res.content
    job.refresh_from_db()
    assert job.status == FieldJob.Status.INVOICED


def test_cancel(admin_client, customer):
    job = FieldJob.objects.create(customer=customer)
    assert admin_client.post(f"/api/field-jobs/{job.id}/cancel/").status_code == 200
    job.refresh_from_db()
    assert job.status == FieldJob.Status.CANCELLED
```

- [ ] **Step 2: Correr los tests para verlos fallar**

Run: `docker compose exec -T backend pytest apps/field_jobs/tests/test_actions.py -q`
Expected: FAIL — `generate-invoice` revienta porque `create_invoice_from_field_job` referencia `job.quintals` (campo ya removido) → AttributeError; `search=corn` puede pasar o no según search_fields.

- [ ] **Step 3: Limpiar el viewset**

En `backend/apps/field_jobs/views.py`, reemplazar la línea de `search_fields` y la del permiso de escritura:

`search_fields` (quitar `applied_product` — puede que ya esté quitado de una tarea previa; dejarlo así):
```python
    search_fields = ["number", "location", "crop", "customer__name"]
```

Permiso de escritura: el spec pide admin/technician/sales. **Importante:** en este código NO existe el
rol literal `"admin"`; los administradores son el grupo `roles.ADMINS = (super_admin, general_admin)`.
Reemplazar la línea `FieldJobWrite = RoleWriteOrReadOnly(*roles.SERVICE_WRITE)` por:
```python
FieldJobWrite = RoleWriteOrReadOnly(*roles.ADMINS, roles.TECHNICIAN, roles.SALES)
```
(Mantener el import `from apps.core import roles`.)

- [ ] **Step 4: Fix de `create_invoice_from_field_job` (sin `quintals`)**

En `backend/apps/billing/services.py`, reemplazar el bloque que arma `quantity`/`unit_word`/`type_word` (rama fumigation/else con `job.quintals`) por una versión solo-fumigación:

```python
    if job.job_type != FieldJob.JobType.FUMIGATION:
        raise ValidationError(
            {"job_type": "La facturación de esparcido aún no está implementada."}
        )
    quantity = job.hectares
    unit_word = "ha"
    type_word = "Fumigación"
    description = (
        f"{type_word} {quantity:.2f} {unit_word} @ ${job.unit_price:.2f}/{unit_word}"
    )
    if job.location:
        description += f" — {job.location}"
```

- [ ] **Step 5: Correr los tests del módulo completo**

Run:
```
docker compose exec -T backend pytest apps/field_jobs/ apps/billing/ -q
```
Expected: todos PASS (incluye test_actions, test_api, test_models, test_mix, test_services, test_products_*, y los de billing).

- [ ] **Step 6: Commit**

```bash
git add backend/apps/field_jobs/views.py backend/apps/billing/services.py backend/apps/field_jobs/tests/test_actions.py
git commit -m "feat(field-jobs): viewset limpio y facturacion solo fumigacion

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

### Task 4: Verificación de la suite completa del backend

**Files:**
- (sin cambios de código; tarea de verificación/regresión)

- [ ] **Step 1: Correr toda la suite del backend**

Run: `docker compose exec -T backend pytest -q`
Expected: toda la suite PASS. Si algún test fuera de `field_jobs`/`billing` referenciaba campos heredados de FieldJob (p. ej. `quintals`, `applied_product`), corregirlo mostrando el cambio mínimo y re-correr.

- [ ] **Step 2: Verificar el endpoint en vivo (sin auth → 401, no 500)**

Run:
```
docker compose exec -T backend python manage.py migrate
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8000/api/field-jobs/
```
Expected: `401` (no 500: confirma que el esquema y la serialización cargan sin la tabla/campos rotos).

- [ ] **Step 3: Commit (si hubo correcciones de regresión)**

```bash
git add -A
git commit -m "test(field-jobs): ajustar regresiones de la suite tras el rebuild

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```
(Si no hubo cambios, omitir este commit.)

---

## Self-Review

- **Spec coverage:** modelo limpio con cultivo/ubicación/cliente/dron/piloto/fecha/hectáreas/precio (Task 1), tope de 10 químicos + crop_display/crop_other + serializer limpio (Task 2), búsqueda/permisos + facturar solo fumigación (Task 3), regresión global (Task 4). Calculadora y FieldJobProduct intactos (spec dice "se conserva"). Reserva de sólidos (enum spreading + unidades kg/ha,g/ha) preservada en el modelo. Cubierto.
- **Placeholder scan:** sin TBD/TODO accionables; el único "TODO conceptual" (rama spreading de `recalculate_total`/facturación) está implementado como guard explícito, no como placeholder.
- **Type consistency:** `crop` choices `rice/corn/pasture/other` consistente entre modelo, tests y serializer; `crop_display` usa `get_crop_display`; productos ≤ 10 validado en `validate_products`; `total` read-only y recalculado en `perform_create/update` (ya existente en el viewset, sin cambios).
- **Nota de migración:** se usa `migrate` (no reset de historial) porque 0001/0002 ya están pusheadas y aplicadas; 0003 transforma el esquema. "Recrear limpio" se cumple a nivel de modelo/esquema resultante.
