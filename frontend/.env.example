# Origen del backend (las rutas del OpenAPI ya incluyen el prefijo /api).
VITE_API_URL=http://localhost:8000

# (Opcional) URL de descarga del APK de la app de campo. Si se define, la tarjeta
# "App de campo" del sidebar se convierte en un botón de descarga. Suele ser el link
# que entrega EAS Build tras `eas build -p android --profile preview`.
# VITE_APK_URL=https://expo.dev/artifacts/eas/xxxxxxxx.apk
